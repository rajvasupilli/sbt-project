pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                sh '''
                     cd /tmp
                     touch firstpipelinefolder
                '''
            }
        }
    }
}
